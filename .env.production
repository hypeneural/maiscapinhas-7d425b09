# Production environment configuration
# This file should be copied to .env on the build server

# API URL - Full URL to the backend API
# In production, the frontend calls the API directly via CORS
VITE_API_URL=https://api.maiscapinhas.com.br/api/v1
